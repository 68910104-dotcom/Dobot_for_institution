🤖 Dobot Color Sorting Project
โปรเจกต์นี้เป็นการควบคุมแขนกล Dobot Magician ให้ทำการหยิบวัตถุ ตรวจสอบสีผ่านกล้อง Webcam และแยกประเภทวัตถุตามสี (แดง, เขียว, น้ำเงิน) ลงในตำแหน่งที่กำหนดไว้อัตโนมัติ โดยใช้ภาษา Python และไลบรารี OpenCV

📋 ฟีเจอร์หลัก
Auto-Connect: ค้นหา Port ของ Dobot และเชื่อมต่ออัตโนมัติ

Computer Vision: ใช้ OpenCV ตรวจจับสีของวัตถุ (ระบบ HSV) โดยวิเคราะห์ภาพจากจุดกึ่งกลาง (ROI)

Sorting Logic: แยกวัตถุไปวางตามตำแหน่งที่กำหนด (Red/Green/Blue) หากไม่รู้จักสีจะวางกลับที่เดิม

Safety Movement: มีการยกแขนขึ้นก่อนเคลื่อนที่ (Z-offset) เพื่อป้องกันการชน

🛠️ อุปกรณ์ที่ต้องใช้ (Hardware)
แขนกล Dobot Magician

หัวจับแบบถ้วยดูด (Suction Cup)

Webcam (ติดตั้งในตำแหน่งที่แขนกลหยิบของมาโชว์ได้)

วัตถุสีต่างๆ (แดง, เขียว, น้ำเงิน)

💻 ซอฟต์แวร์และไลบรารี (Software Requirements)
Python 3.x

Libraries:

opencv-python (สำหรับประมวลผลภาพ)

numpy (สำหรับการคำนวณ array สี)

pydobot (Library สำหรับคุยกับ Dobot)

pyserial (สำหรับการเชื่อมต่อ Serial Port)

การติดตั้ง (Installation)
รันคำสั่งต่อไปนี้ใน Terminal หรือ CMD เพื่อติดตั้งไลบรารีที่จำเป็น:

Bash

pip install opencv-python numpy pydobot pyserial
⚙️ การตั้งค่าพิกัด (Calibration)
สำคัญมาก: ก่อนรันโปรแกรม คุณต้องแก้ไขค่าพิกัด x, y, z ในไฟล์ Color_sorting.py.py ให้ตรงกับสภาพแวดล้อมจริงของคุณ:

pickup_points: ตำแหน่งวางวัตถุที่จะให้ Dobot ไปหยิบ (มี 3 จุด)

camera_point: ตำแหน่งที่ Dobot จะยกวัตถุไปจ่อหน้ากล้องเพื่อให้เห็นชัดที่สุด

drop_points: ตำแหน่งกล่องทิ้งขยะแยกสี (Red, Green, Blue)

Python

# ตัวอย่างการแก้โค้ดส่วนพิกัด
pickup_points = [
    {"x": 200, "y": 0, "z": -50}, # แก้เลขนี้ให้ตรงกับจุดจริง
    ...
]
🚀 วิธีการใช้งาน (Usage)
ต่อสาย USB ของ Dobot และเสียบกล้อง Webcam เข้ากับคอมพิวเตอร์

รันโปรแกรม:

Bash

python Color_sorting.py.py
โปรแกรมจะเชื่อมต่อ Dobot และเคลื่อนไปที่ Start Position

เมนูจะปรากฏขึ้นที่หน้าจอ Terminal:

กด 1 แล้ว Enter: เพื่อเริ่มรอบการทำงาน (หยิบครบ 3 ชิ้น)

กด q แล้ว Enter: เพื่อออกจากโปรแกรม

🔄 ขั้นตอนการทำงานของระบบ
Move to Start: แขนกลรอที่จุดเริ่มต้น

Pick: เคลื่อนที่ไปเหนือวัตถุ -> ลงไปดูด -> ยกขึ้น

Check: เคลื่อนที่ไปหน้ากล้อง -> เปิดกล้องชั่วคราว -> อ่านค่าสี

Sort:

ถ้าเป็นสี แดง/เขียว/น้ำเงิน: นำไปวางในจุดที่กำหนด

ถ้า ไม่รู้จักสี: วางกลับที่เดิม

Loop: ทำซ้ำจนครบ 3 จุดรับของ

Return: กลับไปรอที่จุดเริ่มต้น

⚠️ ปัญหาที่พบบ่อย (Troubleshooting)
หา Dobot ไม่เจอ: เช็คสาย USB หรือเช็คว่า Driver ลงถูกต้องหรือไม่ (เช็คใน Device Manager ว่าขึ้น COM Port หรือไม่)

กล้องเปิดไม่ได้: ลองเปลี่ยนเลขในบรรทัด cv2.VideoCapture(1) เป็น 0 หรือ 2

สีเพี้ยน: ปรับค่าแสงสภาพแวดล้อม หรือแก้ช่วงค่า HSV ในฟังก์ชัน detect_color_from_frame