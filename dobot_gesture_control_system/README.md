# 🤖 Dobot Magician Hand Gesture Control

โปรเจกต์นี้คือระบบควบคุมแขนหุ่นยนต์ **Dobot Magician** ด้วยท่าทางมือ (Hand Gestures) ผ่านกล้อง Webcam โดยใช้ **Python**, **OpenCV**, และ **MediaPipe**

## ✨ ฟีเจอร์หลัก (Features)

- **🔌 Auto-Connection:** ค้นหา Port ของ Dobot โดยอัตโนมัติ ไม่ต้องตั้งค่า COM Port เอง
- **✋ Hand Tracking:** ตรวจจับมือและนิ้วได้อย่างแม่นยำด้วย MediaPipe
- **🎮 Gesture Control:** ควบคุมหุ่นยนต์ด้วยสัญลักษณ์มือ 3 รูปแบบ:
  1. **Movement (✌️):** ขยับแขนหุ่นยนต์ตามการเคลื่อนไหวของนิ้วชี้ (แกน X, Y)
  2. **Grab Sequence (✊):** สั่งให้หุ่นยนต์ดูดของ > ยกขึ้น > ย้ายไปจุดวาง > ปล่อยของ
  3. **Idle/Stop (🖐️):** สั่งหยุดการทำงานและปิดลมดูดทันทีเพื่อความปลอดภัย

## 🛠️ ความต้องการระบบ (Requirements)

### ฮาร์ดแวร์
- หุ่นยนต์ **Dobot Magician** (ติดตั้ง Suction Cup)
- กล้อง Webcam
- สาย USB เชื่อมต่อหุ่นยนต์

### ซอฟต์แวร์
- Python 3.x
- Libraries: `opencv-python`, `mediapipe`, `pydobot`, `pyserial`

## 📦 การติดตั้ง (Installation)

1. **Clone** โปรเจกต์นี้ลงในเครื่องคอมพิวเตอร์
2. **ติดตั้ง Library** ด้วยคำสั่ง:

```bash
pip install opencv-python mediapipe pydobot pyserial
Note: หากพบปัญหาเรื่อง Driver ของ Dobot แนะนำให้ตรวจสอบ pydobot ว่ารองรับ Firmware ปัจจุบันหรือไม่ หรือใช้ Library ที่ Fork มาสำหรับการศึกษา🚀 วิธีการใช้งาน (Usage)เชื่อมต่อ Dobot เข้ากับคอมพิวเตอร์และเปิดสวิตช์รันโปรแกรมด้วยคำสั่ง:Bashpython main.py
ใช้ท่าทางมือควบคุมดังตารางด้านล่าง:ท่าทาง (Gesture)สัญลักษณ์มือการทำงาน (Action)Movement Mode🤟 (3 นิ้ว)ควบคุมทิศทาง: หุ่นยนต์จะขยับตามนิ้วชี้ • เข้า-ออก: ตามแกน Y ของมือ • ซ้าย-ขวา: ตามแกน X ของมือGrab Sequence✊ (กำหมัด)ทำงานอัตโนมัติ: 1. เปิดปั๊มลม (Suck ON) 2. ยกแขนขึ้น 3. ย้ายไปจุดวางของ 4. ปล่อยของ (Suck OFF)Stop / Release🖐️ (แบมือ)หยุด/ปล่อย: • หยุดการเคลื่อนที่ • ปิดปั๊มลมทันที⚙️ การตั้งค่า (Configuration)สามารถแก้ไขค่าพารามิเตอร์ในไฟล์ main.py ได้:พื้นที่ปลอดภัย (Safety Limits):Pythondobot_x = max(120, min(230, dobot_x))  # ระยะยืดสุด-หดสุด (mm)
dobot_y = max(-150, min(150, dobot_y)) # ระยะซ้าย-ขวา (mm)
ตำแหน่งวางของ (Drop Position):Python# แก้ไขพิกัด x, y, z ในโหมด Grab
device.move_to(x=178.96, y=-148.89, z=50, r=0, wait=True)
⚠️ ข้อควรระวัง (Safety)เคลียร์พื้นที่: ตรวจสอบสิ่งกีดขวางในรัศมีแขนหุ่นยนต์ก่อนเริ่มใช้งานแสงสว่าง: ควรใช้ในที่มีแสงสว่างเพียงพอเพื่อให้กล้องจับมือได้ชัดเจนEmergency Stop: กด Esc ที่คีย์บอร์ดเพื่อปิดโปรแกรมทันทีหากเกิดเหตุฉุกเฉิน