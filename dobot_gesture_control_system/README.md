#  Dobot Magician Hand Gesture Control

โปรเจกต์นี้คือระบบควบคุมแขนหุ่นยนต์ **Dobot Magician** ด้วยท่าทางมือ (Hand Gestures) ผ่านกล้อง Webcam โดยใช้ **Python**, **OpenCV**, และ **MediaPipe**

---

##  ฟีเจอร์หลัก (Features)

*  **Auto-Connection**: ค้นหา Port ของ Dobot โดยอัตโนมัติ ไม่ต้องตั้งค่า COM Port เอง
*  **Hand Tracking**: ตรวจจับมือและนิ้วได้อย่างแม่นยำด้วย MediaPipe
*  **Gesture Control**: ควบคุมหุ่นยนต์ด้วยสัญลักษณ์มือ 3 รูปแบบ

  1. **Movement Mode (✌️ / 🤟)** – ขยับแขนหุ่นยนต์ตามการเคลื่อนไหวของนิ้วชี้ (แกน X, Y)
  2. **Grab Sequence (✊)** – ดูดของ → ยกขึ้น → ย้ายไปจุดวาง → ปล่อยของ
  3. **Idle / Stop (🖐️)** – หยุดการทำงานและปิดลมดูดทันทีเพื่อความปลอดภัย

---

## 🛠️ ความต้องการระบบ (Requirements)

### 🔩 ฮาร์ดแวร์

* หุ่นยนต์ **Dobot Magician** (ติดตั้ง Suction Cup)
* กล้อง Webcam
* สาย USB สำหรับเชื่อมต่อหุ่นยนต์

### 💻 ซอฟต์แวร์

* Python 3.x
* Libraries:

  * `opencv-python`
  * `mediapipe`
  * `pydobot`
  * `pyserial`

---

##  การติดตั้ง (Installation)

1. **Clone** โปรเจกต์นี้ลงในเครื่องคอมพิวเตอร์
2. **ติดตั้ง Library** ด้วยคำสั่ง:

```bash
pip install opencv-python mediapipe pydobot pyserial
```

> **Note:** หากพบปัญหา Driver ของ Dobot แนะนำให้ตรวจสอบว่า `pydobot` รองรับ Firmware ปัจจุบันหรือไม่ หรือใช้ Library ที่ Fork มาสำหรับการศึกษา 🚀

---

##  วิธีการใช้งาน (Usage)

1. เชื่อมต่อ Dobot เข้ากับคอมพิวเตอร์ และเปิดสวิตช์
2. รันโปรแกรมด้วยคำสั่ง:

```bash
python main.py
```

3. ใช้ท่าทางมือควบคุมหุ่นยนต์ตามตารางด้านล่าง

###  Gesture Control Table

| ท่าทาง (Gesture) | สัญลักษณ์มือ | การทำงาน (Action)                                                                                          |
| ---------------- | ------------ | ---------------------------------------------------------------------------------------------------------- |
| Movement Mode    | 🤟 (3 นิ้ว)  | ควบคุมทิศทางการเคลื่อนที่ของแขนหุ่นยนต์<br>- ซ้าย–ขวา: แกน X ของมือ<br>- เข้า–ออก: แกน Y ของมือ            |
| Grab Sequence    | ✊ (กำหมัด)   | ทำงานอัตโนมัติ:<br>1) เปิดปั๊มลม (Suck ON)<br>2) ยกแขนขึ้น<br>3) ย้ายไปจุดวางของ<br>4) ปล่อยของ (Suck OFF) |
| Stop / Release   | 🖐️ (แบมือ)  | หยุดการเคลื่อนที่ทันที และปิดปั๊มลม                                                                        |

---

## ⚙️ การตั้งค่า (Configuration)

สามารถแก้ไขค่าพารามิเตอร์ในไฟล์ `main.py` ได้

###  พื้นที่ปลอดภัย (Safety Limits)

```python
dobot_x = max(120, min(230, dobot_x))   # ระยะยืดสุด-หดสุด (mm)
dobot_y = max(-150, min(150, dobot_y)) # ระยะซ้าย-ขวา (mm)
```

###  ตำแหน่งวางของ (Drop Position)

```python
# แก้ไขพิกัด x, y, z ในโหมด Grab
device.move_to(x=178.96, y=-148.89, z=50, r=0, wait=True)
```

---

##  ข้อควรระวัง (Safety)

*  **เคลียร์พื้นที่**: ตรวจสอบสิ่งกีดขวางในรัศมีแขนหุ่นยนต์ก่อนเริ่มใช้งาน
*  **แสงสว่าง**: ควรใช้งานในที่มีแสงเพียงพอ เพื่อให้กล้องตรวจจับมือได้แม่นยำ
*  **Emergency Stop**: กดปุ่ม `Esc` บนคีย์บอร์ดเพื่อปิดโปรแกรมทันทีในกรณีฉุกเฉิน

---

 เหมาะสำหรับใช้เป็นโปรเจกต์ด้าน **Human–Robot Interaction (HRI)**, **Computer Vision**, และ **AI Control System**
